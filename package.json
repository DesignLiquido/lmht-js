{
  "name": "@designliquido/lmht-js",
  "version": "0.0.3",
  "description": "Biblioteca de transformação de documentos LMHT para HTML em JavaScript",
  "main": "index.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DesignLiquido/lmht-js.git"
  },
  "scripts": {
    "empacotar": "rm -Rf ./dist && tsc && copyfiles -V ./README.md ./dist && copyfiles -V ./LICENSE ./dist && copyfiles -V ./package.json ./dist",
    "publicar-npm": "npm publish ./dist --access public"
  },
  "author": "Leonel Sanches da Silva",
  "license": "MIT",
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "dependencies": {
    "saxon-js": "latest"
  },
  "devDependencies": {
    "@types/jest": "^29.0.3",
    "@types/node": "^18.7.18",
    "jest": "^29.0.3",
    "ts-jest": "^29.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  }
}
